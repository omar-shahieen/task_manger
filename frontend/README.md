# Frontend - Task Manager

This is the Vite + React frontend.

Setup:

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

Remember to set `VITE_API_URL` in `.env` if your backend runs on a different port.
